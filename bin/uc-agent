#!/bin/bash

name='mingqi'
# echo $name
# echo ${name:3}
# echo $name

DIR=$(dirname "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )")
$DIR/node/bin/node $DIR/node_modules/ma-agent/lib/cli/daemon.js $*
